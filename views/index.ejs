<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>War on 64 Squares</title> 
    <link rel="stylesheet" href="/css/output.css">
    <style>
        .chessboard {
            display: grid;
            grid-template-columns: repeat(8, 1fr);
            grid-template-rows: repeat(8, 1fr);
            aspect-ratio: 1 / 1;
            width: 90vw;
            max-width: 500px;
            transform: rotate(0deg);
        }

        .piece.white {
            color: white;
            filter: drop-shadow(0 0 2px rgba(0, 0, 0, 1));
        }

        .piece.black {
            color: black;
        }

        .flipped {
            transform: rotate(180deg);
        }

        .square {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .square.light {
            background-color: #f0d9b5;
        }

        .square.dark {
            background-color: #b58863;
        }

        .piece {
            font-size: clamp(30px, 4vw, 40px);
            cursor: pointer;
        }

        .piece.draggable {
            cursor: grab;
        }

        .dragging {
            opacity: 0.5;
            scale: 1.1;
        }

        /* Rotate pieces for black player */
        .flipped .piece {
            transform: rotate(180deg);
        }
    </style>
</head>

<body class="bg-zinc-900 overflow-x-hidden">
    <div class="h-screen p-8 flex flex-col items-center justify-start">
        <h1 class="text-4xl text-white font-bold mb-6 text-center">War on 64 Squares</h1>
        <div class="flex md:flex-row flex-col items-center justify-center bg-zinc-800 p-6 rounded-lg shadow-lg gap-6">
            <div class="flex flex-col items-center bg-zinc-800 p-6 gap-6">
                <h1 class="roomid text-2xl text-white font-bold text-center"></h1>
                <h1 class="playerrole text-2xl text-white font-bold text-center"></h1>
                <h1 class="turnName text-2xl text-white font-bold text-center"></h1>
                <button id="restartBtn"
                    class="mt-4 px-4 py-2 bg-red-600 text-white font-bold rounded hover:bg-red-700 hidden">
                    🔁 Restart Game
                </button>
                <button id="startBtn" class="text-white font-bold">🎙️ Start Voice</button>
                <button id="muteBtn" class="text-white font-bold" disabled>🔇 Mute</button>
            </div>
            <div class=" chessboard">
            </div>
        </div>
    </div>

    <script src="https://cdn.socket.io/4.8.1/socket.io.min.js"
        integrity="sha384-mkQ3/7FUtcGyoppY6bz/PORYoGqOl7/aSUMn2ymDOJcapfS6PHqxhRTMh1RR0Q6+"
        crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"
        integrity="sha512-xRllwz2gdZciIB+AkEbeq+gVhX8VB8XsfqeFbUh+SzHlN96dEduwtTuVuc2u9EROlmW9+yhRlxjif66ORpsgVA=="
        crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script src="/js/chessgame.js"></script>

</body>

</html>